# SSH Access Details

- **Host / IP:** Value passed to `-VmHost` at runtime  
- **Username:** `azureuser`  
- **Private Key:** `$HOME/.ssh/id_rsa`  

## Equivalent SSH Command

```bash
ssh -i ~/.ssh/id_rsa azureuser@<VM_HOST>