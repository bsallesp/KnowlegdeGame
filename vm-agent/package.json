{
  "name": "vm-agent",
  "version": "2.0.0",
  "description": "Dystoppia VM agent — HTTP interface for Claude CLI",
  "main": "server.js",
  "scripts": {
    "start": "node server.js",
    "dev": "node --watch server.js"
  },
  "dependencies": {
    "dotenv": "^16.0.0",
    "express": "^4.18.0"
  },
  "engines": {
    "node": ">=18"
  }
}
