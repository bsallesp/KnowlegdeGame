// Credits were replaced by lib/rateLimit.ts — see __tests__/lib/rateLimit.test.ts.
import { describe, test, expect } from "vitest";

describe("credits (deprecated)", () => {
  test("rate limit coverage lives in rateLimit.test.ts", () => {
    expect(true).toBe(true);
  });
});
